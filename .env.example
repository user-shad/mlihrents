SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Cloud sync API — same secret on server (SYNC_API_TOKEN) and frontend build (VITE_SYNC_API_TOKEN)
SYNC_API_TOKEN=
VITE_SYNC_API_TOKEN=

# Optional — direct browser realtime (not required if using API sync above)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
